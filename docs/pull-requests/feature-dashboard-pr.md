---
title: "feat: enhance dashboard with spending stats and add public landing page"
base: main
compare: feature/dashboard
date: 2026-03-26
---

## Summary
- Significantly expands the dashboard with spending statistics, a top-categories breakdown, and a full recent-expenses table
- Adds a public-facing landing page at `/` that introduces the app to unauthenticated visitors
- Replaces the placeholder home page content with a polished hero section and feature highlights

## Changes

**Dashboard**
- `components/dashboard/DashboardView.tsx` — rebuilt with summary stat cards (total spend, expense count, top category, monthly average), a top-5 category breakdown table with percentage bars, and a paginated recent expenses table with category chips
- `app/(app)/dashboard/page.tsx` — updated data fetching to pass all expenses and categories to the view

**Public landing page**
- `app/(public)/page.tsx` — new marketing page with hero section, feature grid, and call-to-action buttons linking to sign-in and register

## How to test
1. Navigate to `http://localhost:3000` while unauthenticated — the landing page should render
2. Click "Get started" and "Sign in" links — confirm they route to `/register` and `/login` respectively
3. Sign in and navigate to `/dashboard`
4. Confirm the stat cards display correct values based on your expense data
5. Confirm the top-categories table lists up to five categories sorted by spend, with percentage bars that sum to 100%
6. Confirm the recent expenses table shows the latest entries with amount, category chip, and date
7. Add a new expense and return to the dashboard — confirm the stats update

---
*Generated by /create-pr on 2026-03-26*
