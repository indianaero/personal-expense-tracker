---
title: "feat: add reports page, Docker config, and GitHub Actions CI/CD pipeline"
base: main
compare: feature/dashboards-charts
date: 2026-03-29
---

## Summary
- Adds a `/reports` page with interactive Chart.js visualisations (category doughnut chart
  and spending bar chart) with period filtering and full light/dark mode support
- Introduces a production-ready Docker configuration with a multi-stage build and
  non-root runtime user
- Sets up GitHub Actions pipelines that automatically deploy preview builds to Vercel
  on every PR and deploy to production on merge to main
- Fixes all lint errors and TypeScript issues so the build pipeline is clean from the
  first run

## Changes

**Reports feature**
- `app/(app)/reports/` — server page and loading skeleton
- `components/reports/ReportsView.tsx` — stat cards, period selector, charts, and category breakdown table
- `components/charts/` — CategoryDoughnutChart, SpendingBarChart, ChartSkeleton components
- `lib/charts/` — currency formatters and consistent colour palette
- `app/providers.tsx` — adds ThemeProvider for system/light/dark switching
- `components/layout/AppNavbar.tsx` — Reports nav link
- `docs/charts.md` — chart conventions, palette usage, and theming rules

**Docker and deployment config**
- `Dockerfile` — three-stage build (deps → builder → runner) with non-root user
- `.dockerignore` — excludes secrets, build artefacts, and dev files from the build context
- `vercel.json` — framework config, security headers, and immutable static asset caching
- `.nvmrc` — pins Node 20 across CI and local environments
- `next.config.ts` — adds `output: 'standalone'` and seven security response headers

**GitHub Actions CI/CD**
- `.github/workflows/preview.yml` — lint → type-check → Docker build → Vercel preview deploy → PR comment with URL
- `.github/workflows/production.yml` — same gates → Vercel production deploy on push to main

**Code quality fixes**
- `__tests__/lib/auth/config.test.ts` — cast `SessionCallback` result as `Session` to satisfy TypeScript
- `__tests__/middleware.test.ts` — replaced bare `Function` types with explicit signatures
- `components/categories/CategoriesView.tsx` — moved `EmptyState` outside the render function
- `eslint.config.mjs` — added `coverage/**` to ignored paths
- Various files — removed unused imports (`Listbox`, `ListboxItem`, `Errors`, `JWT`)

**Documentation and tooling**
- `docs/deployment.md` — full deployment specification covering both flows
- `docs/pull-requests/` — historical PR documents for all five previously merged features
- `CLAUDE.md` — added `docs/deployment.md` to mandatory reading table
- `.claude/commands/create-pr.md` — `/create-pr` custom slash command

## How to test
1. Run `npm run lint` — expect 0 errors, 0 warnings
2. Run `npx tsc --noEmit` — expect 0 errors
3. Run `npm test` — expect 52/52 passing
4. Run `npm run build` — expect a clean build; confirm `.next/standalone/server.js` exists
5. Sign in with an account that has expense data and navigate to `/reports`
6. Verify the stat cards show correct totals and the doughnut and bar charts render
7. Switch between Weekly / Monthly / Yearly filters and confirm charts update
8. Toggle light/dark mode and confirm charts re-colour without a page reload
9. Open a pull request on GitHub — confirm the preview workflow triggers, runs all gates,
   and posts a preview URL comment on the PR
10. Merge to main — confirm the production workflow triggers and completes successfully

## Notes
- Three repository secrets must be added before the workflows can run: `VERCEL_TOKEN`,
  `VERCEL_ORG_ID`, and `VERCEL_PROJECT_ID` — full setup instructions are in
  `docs/deployment.md` § 8
- `next.config.ts` now sets `output: 'standalone'`; Vercel handles this natively with
  no extra configuration required

---
*Generated by /create-pr on 2026-03-29*
