---
title: "feat: add reports page with spending charts and dark mode support"
base: main
compare: feature/dashboards-charts
date: 2026-03-26
---

## Summary
- Adds a `/reports` page that visualises user expense data as interactive charts
- Implements a doughnut chart for category breakdown and a bar chart for spending over time
- Introduces period filtering (weekly/monthly/yearly) so users can explore spending trends
- Adds light/dark mode support via `next-themes` with charts that adapt to the active theme

## Changes

**New page & loading state**
- `app/(app)/reports/page.tsx` — server component fetching user expenses and categories, guarded by auth
- `app/(app)/reports/loading.tsx` — skeleton loading UI matching the reports layout

**Chart components**
- `components/charts/CategoryDoughnutChart.tsx` — Chart.js doughnut chart showing spending by category
- `components/charts/SpendingBarChart.tsx` — Chart.js bar chart showing spending over time
- `components/charts/ChartSkeleton.tsx` — shared skeleton placeholder for charts

**Reports UI**
- `components/reports/ReportsView.tsx` — orchestrates stat cards, period selector, charts, and category breakdown table

**Chart utilities**
- `lib/charts/formatters.ts` — currency formatting helpers
- `lib/charts/palette.ts` — consistent color palette for chart segments

**Theme & providers**
- `app/providers.tsx` — wraps app with `ThemeProvider` for system/light/dark theme switching

**Nav & config**
- `components/layout/AppNavbar.tsx` — added Reports nav link
- `next.config.ts`, `package.json` — added `react-chartjs-2`, `chart.js`, `next-themes` dependencies

**Documentation**
- `docs/charts.md` — comprehensive guide for chart conventions, palette usage, and theming rules

## How to test
1. Log in with a valid user account
2. Add several expenses across different categories and dates
3. Navigate to `/reports` via the navbar
4. Verify the stat cards show correct total spend, expense count, and top category
5. Switch between Weekly / Monthly / Yearly period filters and confirm charts update
6. Verify the doughnut chart segments correspond to category totals
7. Verify the bar chart shows spending aggregated by the selected period
8. Toggle between light and dark mode and confirm charts adapt without a page reload
9. Verify the loading skeleton appears briefly on first load

---
*Generated by /create-pr on 2026-03-26*
